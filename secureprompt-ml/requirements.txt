fastapi==0.115.14
uvicorn[standard]==0.29.0
pydantic==2.11.9
cryptography>=42
presidio-analyzer==2.2.362
presidio-anonymizer==2.2.362
# GLiNER2-PII is the default English NER backend (ML_NER_BACKEND=gliner2).
# It requires transformers>=5, which is incompatible with the legacy
# gliner 0.2.x (pins transformers<5) — so the old `gliner` package and the
# presidio [gliner] extra are intentionally dropped.
gliner2==1.3.2
spacy==3.8.2
transformers==5.6.2
sentence-transformers==5.4.1
onnxruntime==1.20.1
numpy==1.26.4
qdrant-client==1.17.1
# Optional extractors for /v1/scan-file — the handler falls back to raw
# decoding if these are missing so rebuilds without them still work.
pypdf==5.1.0
python-docx==1.1.2
python-multipart==0.0.12
# torch CPU-only installed separately in Dockerfile:
# pip install torch==2.6.0+cpu --extra-index-url https://download.pytorch.org/whl/cpu

//! Task 5-01-C — `/v1/auth/{token,refresh,logout}` handler tests.
//!
//! Populated by Task 5-01-C once the handlers exist. Registered by
//! `dashboard/mod.rs` now so the crate compiles during Tasks 5-01-A and
//! 5-01-B.

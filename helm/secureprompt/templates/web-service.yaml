apiVersion: v1
kind: Service
metadata:
  name: {{ include "secureprompt.fullname" . }}-web
  labels:
    {{- include "secureprompt.labels" . | nindent 4 }}
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: {{ include "secureprompt.fullname" . }}-web
